import React, { Component } from 'react'
import ObjectInspector from 'react-object-inspector'

export const ObjectViewer = ({obj}) => {
  return (
    <div className="type-instance">
      <div className="name">{obj.name}</div>
      <div className="hash">{obj.hashStruct()}</div>
      <ObjectInspector data={obj.toObject()} />
    </div>
  )
}

export const ObjectCreator = ({create}) => {
  return (
    <div className="">

    </div>
  )
}