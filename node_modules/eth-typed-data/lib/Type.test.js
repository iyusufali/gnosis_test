"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _Type = _interopRequireDefault(require("./Type"));

describe('Type factory', function () {
  it('Creates a new class with sign, encode, toObject, and toSignatureRequest methods', function () {
    var Empty = _Type.default.call({
      types: {}
    }, 'Empty', []);

    expect(Empty.hasOwnProperty('sign')).toBe(true);
    expect(Empty.hasOwnProperty('encode')).toBe(true);
    expect(Empty.hasOwnProperty('toObject')).toBe(true);
    expect(Empty.hasOwnProperty('toSignatureRequest')).toBe(true);
  });
});